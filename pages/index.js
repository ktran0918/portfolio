import { Global, css } from "@emotion/core";
import Layout from "../components/Layout";

export default function Index() {
  return (
    <React.Fragment>
      <Layout>
        <h1>Welcome to Khoa's portfolio!</h1>
      </Layout>
    </React.Fragment>
  );
}